<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>
      Sorry, we can&rsquo;t find that page
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1">
    <link rel="stylesheet" href="//fonts.googleapis.com/css?family=Open%20Sans:300,400" type="text/css">
    <style type="text/css">

      html,
      body{
        height : 100%;
      }

      body{
        margin      : 0;
        padding     : 0;
        background  : rgb(240,240,240);
        color       : rgb(64,64,64);
        font-family : 'Open Sans',Arial,Helvetica,sans-serif;
        font-size   : 16px;
        line-height : 1.5;
      }

      body div{
        display   : table;
        max-width : 512px;
        height    : 100%;
        margin    : 0 auto;
      }

      body div div{
        display        : table-cell;
        vertical-align : middle;
      }

      h1{
        margin      : 0 0 16px;
        padding     : 0;
        font-size   : 32px;
        font-weight : 300;
        text-align  : center;
      }

      p{
        margin       : 0;
        padding      : 16px 0;
        border       : solid rgb(224,224,224);
        border-width : 1px 0;
      }

      form{
        margin  : 24px 0;
        padding : 0;
      }

      body form div{
        display    : block;
        height     : auto;
        text-align : center;
      }

      button{
        margin      : 0;
        padding     : 0;
        border      : 0;
        background  : transparent;
        font-family : inherit;
        font-size   : 1em;
        cursor      : pointer;
      }

      button::-moz-focus-inner{
        padding : 0;
        border  : 0;
      }

      button span{
        display             : block;
        padding             : 2px 8px;
        border              : 1px solid rgb(224,224,224);
        border-bottom-color : rgb(208,208,208);
        border-radius       : 4px;
        background          : rgb(232,232,232);
        background-image    : -webkit-linear-gradient(rgb(240,240,240),rgb(232,232,232));
        background-image    :         linear-gradient(rgb(240,240,240),rgb(232,232,232));
        color               : rgb(64,64,64);
        text-shadow         : 0 0 2px white;
        line-height         : 24px;
        box-shadow          : inset 0 1px 0 rgba(255,255,255,0.5);
      }

    </style>
  </head>
  <body>
    <div>
      <div>
        <h1>Sorry, we can&rsquo;t find that page</h1>
        <p>
          We may have moved the information you&rsquo;re looking for to another page on our site.
          Try visiting our home page to see if we can help.
        </p>
        <form action="../" method="GET">
          <div>
            <button><span>Go to our home page</span></button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
